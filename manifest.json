{
    "name": "Copy Link as Markdown",
    "version": "1.0",
    "manifest_version": 3,
    "description": "Adds a context menu item to copy the current page (or link) as a Markdown link.",
    "permissions": [
        "contextMenus",
        "activeTab",
        "scripting"
    ],
    "background": {
        "service_worker": "background.js"
    },
    "commands": {
        "copy_link_as_markdown": {
            "suggested_key": {
                "default": "Alt+Shift+M"
            },
            "description": "Copy the current tab as a Markdown link"
        }
    }
}